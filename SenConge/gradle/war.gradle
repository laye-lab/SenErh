apply plugin: "war"

bootWar {
    mainClassName = "com.seneau.senerh.SenCongeApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
